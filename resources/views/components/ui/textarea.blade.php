<label for="{{ $name }}">{{ $label }}</label>
<textarea
    id="{{ $id }}"
    name="{{ $name }}"
    placeholder="{{ $placeholder }}"
    cols="{{ $cols }}"
    rows="{{ $rows }}">{{ $value }}</textarea>